import { styles } from "../styles";

const Travels = () => {
    return (
        <div className="md:m-12 md:px-48 flex flex-col sm:flex-row gap-10 overflow-hidden text-center justify-center">
            <h1 className={styles.sectionText}>
              Travels (WIP)
            </h1>
        </div>
    );
  };
  
  export default Travels;